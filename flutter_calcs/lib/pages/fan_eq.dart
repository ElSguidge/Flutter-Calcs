import 'package:flutter/material.dart';

class FanEquations extends StatelessWidget {
  const FanEquations({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
