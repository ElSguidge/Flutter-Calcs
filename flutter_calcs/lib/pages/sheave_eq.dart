import 'package:flutter/material.dart';

class SheaveEquations extends StatelessWidget {
  const SheaveEquations({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
