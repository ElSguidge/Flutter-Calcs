import 'package:flutter/material.dart';

class HydronicHome extends StatelessWidget {
  const HydronicHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
