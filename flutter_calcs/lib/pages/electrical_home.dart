import 'package:flutter/material.dart';

class ElectricalHome extends StatelessWidget {
  const ElectricalHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
